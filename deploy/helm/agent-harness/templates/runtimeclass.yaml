{{- if .Values.sandbox.createRuntimeClass }}
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: {{ .Values.sandbox.runtimeClassName }}
handler: {{ .Values.sandbox.runtimeHandler }}
{{- end }}
